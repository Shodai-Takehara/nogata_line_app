<!DOCTYPE html>
<html>
  <head>
    <title>LIFF App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <!-- LIFF ID: <%= @liff_id %> -->
    <meta name="liff-id" content="<%= @liff_id %>">
    <% if @dev_mode %>
    <meta name="dev-mode" content="true">
    <% end %>
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <% unless @dev_mode %>
    <script charset="utf-8" src="https://static.line-scdn.net/liff/edge/2/sdk.js"></script>
    <% end %>

    <script>
      // デバッグ情報の表示
      console.log('CSRF Token:', document.querySelector('meta[name="csrf-token"]')?.content);
    </script>
  </head>

  <body class="bg-gray-100 min-h-screen">
    <% if flash[:notice] %>
      <div class="bg-green-100 border border-green-300 text-green-700 px-4 py-3 mb-4 rounded relative" role="alert">
        <span class="block sm:inline"><%= flash[:notice] %></span>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="bg-red-100 border border-red-300 text-red-700 px-4 py-3 mb-4 rounded relative" role="alert">
        <span class="block sm:inline"><%= flash[:alert] %></span>
      </div>
    <% end %>

    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>
